# Server
PORT=3001
NODE_ENV=development
APP_URL=http://localhost:3000
API_URL=http://localhost:3001

# Supabase Direct connection
DATABASE_URL="postgresql://postgres:[AED9PGU+4s+afZq]@db.wghpizhhssjdllrvpeiv.supabase.co:5432/postgres"

# JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_REFRESH_SECRET=change-me-to-another-long-random-string
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Mock mode (true = SMS va to'lov mock)
MOCK_PAYMENTS=true

# Commission (%)
COMMISSION_RATE=13

# Pending order auto-cancel (minutes)
ORDER_ACCEPT_TIMEOUT_MINUTES=30
ORDER_EXPRESS_ACCEPT_TIMEOUT_MINUTES=15
ORDER_PAYMENT_TIMEOUT_MINUTES=20

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:8081

# Web Push (VAPID) — npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@ustatop.uz

# Telegram bot (usta / mijoz buyurtma xabarnomalari)
TELEGRAM_BOT_TOKEN=

# Admin alert bot (xato, API, sahifa muammolari, takliflar)
# Botni ochib /start yuboring — chat avtomatik ulanadi
TELEGRAM_ADMIN_BOT_TOKEN=
# Ixtiyoriy: vergul bilan bir nechta chat ID
TELEGRAM_ADMIN_CHAT_ID=
